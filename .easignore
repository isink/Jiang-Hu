# Ignore local node_modules, as EAS will install them in the cloud
node_modules/

# Ignore local build artifacts
.expo/
android/
ios/
dist/

# Ignore large unused assets
assets/未命名文件夹/

# Ignore sensitive files (they should be in eas.json or EAS Secret)
.env
.env.local

# Ignore large media if necessary (but keep app assets)
# *.mp4
# *.mov

# Standard git ignores
.git
.gitignore
.vscode
